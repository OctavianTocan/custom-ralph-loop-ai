# Ralph session files (user data - ignore all session content)
sessions/*/ralph.log
sessions/*/.ralph.lock
sessions/*/prd.json
sessions/*/progress.txt
sessions/*/learnings.md
sessions/*/SESSION-SUMMARY.md
sessions/*/CLAUDE.md

# Keep session directory structure
!sessions/.gitkeep
!sessions/

# Ignore any project-specific files
*.log
prd.json
progress.txt
CLAUDE.md

# Release artifacts (created by CI, not stored in git)
*.tar.gz
*.zip
ralph-v*.tar.gz
ralph-v*.zip
